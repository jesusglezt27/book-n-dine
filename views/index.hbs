
<ul> 
    {{#each restaurants}}
    <div class="card">
        <img src="{{restaurant.images}}" alt="Card image">
        <div class="card-body">
            <h4 class="card-title">{{name}}</h4>
            <p class="card-text">{{description}}</p>
            <a href="/restaurant/{{_id}}/detail" class="btn btn-primary">Ver más</a>
        </div>
    </div>
    {{/each}}
</ul>